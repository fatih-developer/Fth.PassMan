@model PassShowViewModel

<div class="card" style="width: 18rem;">

    @{
        var base64 = Convert.ToBase64String(Model.imageData);
        var imgSrc = String.Format("data:image/png;base64,{0}", base64);
    }

    <img src="@imgSrc" class="card-img-top w-75 text-center justify-content-center" alt="...">

    @if (@Model.Pass != "")
    {
        <h2 class="card-text text-danger text-center text-sm-center">@Model.Pass</h2>
    }

    <div class="card-body">
        <h5 class="card-title">@Model.Passwords.Loginname</h5>
        <p class="card-text">Username = @Model.Passwords.Username / @Model.Passwords.Detail</p>
        <p class="text-black-50">@Model.Passwords.Category[0]</p>
        <p class="text-black-50">@Model.Passwords.Clients[0]</p>
    </div>
    
    <div class="card-footer">
        <a asp-controller="Password" asp-action="Details" asp-route-visible="true" class="card-link">Parola Göster</a>
    </div>
</div>